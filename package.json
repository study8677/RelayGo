{
  "name": "relaygo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "check": "node --check src/app.js && node --check src/data.js && node --check server.js"
  },
  "devDependencies": {
    "playwright": "^1.55.1"
  }
}
